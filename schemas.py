from pydantic import BaseModel, EmailStr\nfrom typing import Optional\nfrom datetime import datetime\n\n# ============ USER SCHEMAS ============\n\nclass UserRegister(BaseModel):\n    username: str\n    email: EmailStr\n    password: str\n\nclass UserLogin(BaseModel):\n    username: str\n    password: str\n\nclass UserResponse(BaseModel):\n    id: int\n    username: str\n    email: str\n    balance: float\n    created_at: datetime\n    \n    class Config:\n        from_attributes = True\n\nclass UserProfileUpdate(BaseModel):\n    email: Optional[EmailStr] = None\n    password: Optional[str] = None\n\n# ============ APOSTA SCHEMAS ============\n\nclass ApostaCreate(BaseModel):\n    evento: str\n    tipo: str  # vitória, empate, derrota\n    odd: float\n    valor: float\n\nclass ApostaResponse(BaseModel):\n    id: int\n    user_id: int\n    evento: str\n    tipo: str\n    odd: float\n    valor: float\n    possivel_ganho: float\n    status: str\n    created_at: datetime\n    \n    class Config:\n        from_attributes = True\n\nclass ApostaUpdate(BaseModel):\n    status: str  # ganha, perde, cancelada\n\n# ============ RESULTADO SCHEMAS ============\n\nclass ResultadoCreate(BaseModel):\n    evento: str\n    resultado: str\n\nclass ResultadoResponse(BaseModel):\n    id: int\n    evento: str\n    resultado: str\n    criado_em: datetime\n    \n    class Config:\n        from_attributes = True\n\n# ============ TRANSACAO SCHEMAS ============\n\nclass TransacaoResponse(BaseModel):\n    id: int\n    user_id: int\n    tipo: str\n    valor: float\n    descricao: Optional[str]\n    saldo_anterior: float\n    saldo_novo: float\n    created_at: datetime\n    \n    class Config:\n        from_attributes = True\n\n# ============ TOKEN SCHEMAS ============\n\nclass Token(BaseModel):\n    access_token: str\n    token_type: str\n    user: UserResponse\n\nclass TokenData(BaseModel):\n    username: Optional[str] = None